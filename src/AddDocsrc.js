import React from 'react';
import {StyleSheet, Text} from 'react-native';

const AddDocsrc =()=>{
    return(<Text>Add Docs Screen</Text>);
}
const styles = StyleSheet.create({

});

export default AddDocsrc;